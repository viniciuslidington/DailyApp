export default function Home() {
  return (
    <main className="min-h-dvh flex items-center justify-center p-10">
      <div className="text-center">
        <p className="text-sm tracking-wide uppercase text-ink-3 mb-2">Daily</p>
        <h1 className="text-2xl font-semibold text-ink">Setup in progress</h1>
        <p className="mt-3 text-sm text-ink-2 max-w-sm">
          Foundation is wired. Drop the design source into{" "}
          <code className="font-mono">docs/App onboarding/</code> and start Phase 2.
        </p>
      </div>
    </main>
  );
}
